## Agent skills

### Product spec

产品需求以 `.scratch/morss/spec.md` 为准；设计、实现或验收功能前，读取相关条款并区分已确认与待决内容。

### Issue tracker

任务使用 `.scratch/` 下的本地 Markdown；创建、读取或更新任务与功能规格前，阅读 `docs/agents/issue-tracker.md`。

### Triage labels

任务分诊采用五个默认角色；分诊或更新任务状态前，阅读 `docs/agents/triage-labels.md`。

### Domain docs

采用 single-context 布局；探索代码或设计领域模型前，阅读 `docs/agents/domain.md`。
